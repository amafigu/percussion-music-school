import React from "react";
import styles from "./courses.module.scss";

const Courses = () => {
  return (
    <div className={styles.coursesPageWrapper}>
      <div className={styles.coursesPage}>Courses</div>
    </div>
  );
};

export default Courses;
